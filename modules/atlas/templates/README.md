# Atlas Templates

This directory is reserved for optional Atlas templates referenced by `modules/atlas/wiki.json`.

Current tools do not require template files to exist for core indexing, validation, search, or apply flows.
It is kept in the repository so different agents and future workflows resolve `template_dir` consistently.
